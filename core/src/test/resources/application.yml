spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pareto?currentSchema=core_test
    username: pareto
    password: pareto
    driver-class-name: org.postgresql.Driver
  flyway:
    schemas: core_test
  process:
    starter:
      delay: 50
